export type User = {
    id: string,
    companyId: string,
    name: string,
    email: string,
    password: string,
    photo: string,
};

export type Company = {
    id: string,
    name: string,
};
